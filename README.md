# InvitacionWeb
Proyecto web hecho con Flask con el objetivo de crear una invitacion web para un evento.

Para crear el entorno :
python3 -m venv .venv

Para correr el entorno virtual:
source .venv/bin/activate

Correr la pagina de forma local:
flask run